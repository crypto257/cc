pip install fastapi uvicorn

app.py:
from fastapi import FastAPI

app = FastAPI()

@app.get("/hello")
def hello(name: str = "World"):
    return {"message": f"Hello {name} from Python API"}



in cmd: python -m uvicorn app:app --reload --port 8000

search in google:
http://localhost:8000/hello
http://localhost:8000/hello?name=YourName

in Visual Studio 2019→ console app (.net core) & name: ClientApp

program.cs:
using System;
using System.Net.Http;
using System.Threading.Tasks;

class Program
{
    static async Task Main()
    {
        using var client = new HttpClient();

        var response = await client.GetAsync("http://localhost:8000/hello?name=Tanishka");
        var content = await response.Content.ReadAsStringAsync();

        Console.WriteLine("Response from Python API:");
        Console.WriteLine(content);
    }
}
